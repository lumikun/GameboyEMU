# TODO this will be changed eventually. for Now this stays.

project('gameboyemu', 'c',
                      default_options: 'default_library=static')

sdl2_dep = dependency('sdl2')

executable('gameboyemu', 'main.c',
                         dependencies: sdl2_dep)
